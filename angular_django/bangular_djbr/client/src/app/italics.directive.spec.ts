import { ItalicsDirective } from './italics.directive';

describe('ItalicsDirective', () => {
  it('should create an instance', () => {
    const directive = new ItalicsDirective();
    expect(directive).toBeTruthy();
  });
});
